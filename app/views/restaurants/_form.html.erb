<%= simple_form_for (restaurant) do |f| %>
    <%= f.input :name  %>
    <%= f.input :address %>
    <%= f.input :category, collection: %w(chinese italian japanese french belgian), include_blank: false %>
    <%= f.submit  %>
<% end %>
